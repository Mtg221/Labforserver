# Backend Basics – Express Lab

## How to Run
1. Install dependencies:
   npm install
2. Start server:
   node server.js

Server runs on http://localhost:3000

## Endpoints
- GET / → Welcome message
- GET /about → App description
- GET /api/users → List of users (JSON)
- POST /api/login → Simple authentication
